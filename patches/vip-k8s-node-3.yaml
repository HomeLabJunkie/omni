# Apply this path using omnictl apply -f vip-k8s-node-3.yaml
# Optain the patch with omnictl get -o yaml configpatch <UUID of Patch in omni dashboard> > outputpatch.yaml

metadata:
    namespace: default
    type: ConfigPatches.omni.sidero.dev
    id: 500-38ef4e51-7b92-45a2-bb08-2a0bd762b3b5
    labels:
        omni.sidero.dev/machine: 6680f42c-8187-0cac-dc4c-4f836fe99873
    annotations:
        description: add vip to ControlPlane node
        name: add vip
spec:
    data: |-
        machine:
          network:
            interfaces:
              - deviceSelector:
                  physical: true # should select any hardware network device, if you have just one, it will be selected
                dhcp: true
                vip:
                  ip: 192.168.1.216
    compresseddata: []
